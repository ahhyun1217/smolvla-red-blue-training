policy:
  type: smolvla
  device: cuda
  push_to_hub: true
  repo_id: AmberHyunKIM/smolvla_red_only
  freeze_vision_encoder: false
  train_expert_only: false
  scheduler_decay_steps: 38000

dataset:
  repo_id: AmberHyunKIM/so101_red_only
  image_transforms:
    enable: false

output_dir: outputs/train/smolvla_red_only
job_name: smolvla_red_only
batch_size: 8
steps: 38000
save_freq: 5000
wandb:
  enable: false
